version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=CoinKit_Identity
      - CoinKit_DB=CoinKit_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
